services:
  frontend:
    image: 265751834194.dkr.ecr.ap-south-1.amazonaws.com/profiilo-fe:latest
    container_name: profilio-frontend

    ports:
      - "127.0.0.1:3000:80"

    restart: unless-stopped

    networks:
      - profilio-network

networks:
  profilio-network:
    external: true